export enum ETagPaint {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  YELLOW_PAINT = 'yellow-paint',
  GREEN_PAINT = 'green-paint',
}

export enum ETagLine {
  GREEN = 'green',
  BLUE = 'blue',
  YELLOW = 'yellow',
}
